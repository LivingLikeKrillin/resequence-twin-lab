# Test overrides — loaded by Spring Boot test context automatically.
# Keep the scheduler disabled so tick() never fires against a non-existent Ditto
# during unit/CI runs. contextLoads and all pure tests must stay green without Docker.
pbs.twin.enabled=false

# Drift monitor disabled in tests: scheduler no-ops, no live source polling (S5).
pbs.drift.enabled=false
